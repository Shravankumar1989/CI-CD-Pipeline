apiVersion: v1
kind: Service
metadata:
  name: spring-boot-app-service
  namespace: default
spec:
  type: ClusterIP   # Changed service type to ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8888
    protocol: TCP
  selector:
    app: spring-boot-app
